<Window x:Class="Builders.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Builders.Views"
        xmlns:locaVM="clr-namespace:Builders.ViewModels"
        mc:Ignorable="d"
        Title="{Binding NameWindow}" 
        MinHeight="500"
        MinWidth="1000"
        Height="500" 
        Width="1000" 
        WindowState="Maximized"
        Style="{StaticResource WindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <local:MenuControl Grid.Row="0"/>
        <local:TabControl Grid.Row="1" 
                          Opacity="{Binding OpacityProgress}"/>
        <local:ProgressBarControl Grid.Row="1" 
                                  Visibility="{Binding Progress}"/>
        
       
    </Grid>
</Window>
